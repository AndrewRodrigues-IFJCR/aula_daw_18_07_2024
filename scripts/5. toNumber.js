// 5. Escreva uma função que converta uma string em um número.

/** 
 * Essa função converte um a string numérica para um número o tipo numérico.
 * @param {String} string É a string contendo o número que sera convertido.
 * @returns {Number} 
*/

function toNumber(string) {
    return Number(string);
}