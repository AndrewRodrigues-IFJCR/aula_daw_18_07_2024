// 4. Escreva uma função que conte o número de caracteres em uma string.

/** 
 * Essa função retorna o tamanho/comprimento de uma string qualquer.
 * @param {String} string É a string a ser medida.
 * @returns {Number} 
*/

function getLength(string) {
    return string.length
}